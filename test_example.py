def test_dummyUnitTest():
    # without any unit tests the ci is failing
    # unit tests should be used later on at higher code complexity
    # they are used to verify that core functionalities are not damaged by code changes
    assert 2 + 3 == 5

